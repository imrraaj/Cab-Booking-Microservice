syntax = "proto3";

package ridePackage;

service RideService {
  rpc calculateRidePrice (RideRequest) returns (RideResponse);
}

message RideRequest {
  string riderId = 1;
  string driverId = 2;
  string carType = 3;
  double distance = 4;
}

message RideResponse {
  double price = 1;
}
